export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
    UNDISCLOSED = "UNDISCLOSED",
}

export enum BusinessType {
    RETAIL = "RETAIL",
    HOSPITALITY = "HOSPITALITY",
   
}

export enum BusinessType {
    RETAIL = "RETAIL",
    HOSPITALITY = "HOSPITALITY",
   
}
